// Version constant lives in Core (chord style: ChordVersion.current) so
// tests can assert on it. Keep in sync with the next release.yml draft
// version when publishing (CLAUDE.md "Conventions").

public enum JigVersion {
    public static let current = "0.1.0"
}
